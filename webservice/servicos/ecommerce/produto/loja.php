<?php

    // $criterio = tdc::f();
    // $criterio->isTrue('');
    // $criterio->onlyActive();

    // $retorno ['data'] = tdc::da('td_ecommerce_produto',$criterio);
    //var_dump(tdc::da('td_ecommerce_produto'));
    $retorno['data'] = tdc::da('td_ecommerce_produto');